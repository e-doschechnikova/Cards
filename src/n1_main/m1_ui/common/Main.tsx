import React from "react";
import {Header} from "../header/Header";
import {Pages} from "../routes/Pages";

export const Main = () => {
    return (
        <div>
            <Header/>
            <Pages/>
        </div>
    );
};